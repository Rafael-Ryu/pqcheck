# Byte-compiled / optimized
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# uv
.venv/
venv/
.python-version.local

# Tests / coverage
.coverage
.coverage.*
.cache
.pytest_cache/
.hypothesis/
htmlcov/
coverage.xml
*.cover
.mypy_cache/
.ruff_cache/

# tree-sitter grammars (build artifacts; checked-in .so files allowed under src/pqcheck/grammars/)
src/pqcheck/grammars/*.dylib
src/pqcheck/grammars/*.dll

# bundled binaries (built per-platform via cibuildwheel)
src/pqcheck/bin/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local environment
.env
.env.local
.direnv/

# Local agent / harness state
.claude/
.codex/

# Local-only planning & agent context (never commit — keep on disk only)
.context/
autoresearch/
docs/
CLAUDE.md
tests/corpus/.cache/
graphify-out/
